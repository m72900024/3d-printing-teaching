# Course 06 A1 First Print Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn Course 06 into a practical Bambu Lab A1 first-print lesson covering setup, sending, first-layer diagnosis, safe stopping, cooling, removal, and official follow-up sources.

**Architecture:** Keep `course-data.js` as the single lesson-content source and use the existing `steps`, `compare`, `callout`, and `sources` render paths in both `course.js` and `scripts/build-site.js`. Add a focused static-build regression test first, then make the smallest Course 06-only data change that satisfies it.

**Tech Stack:** Static HTML/CSS/JavaScript, Node.js built-in test runner, existing zero-dependency site builder.

## Global Constraints

- Course 06 uses Bambu Lab A1 as the only demonstration machine.
- The shared first-print setup is PLA, a 0.4 mm nozzle, Textured PEI Plate, and the 0.20 mm Standard preset.
- Retain the existing Course 06 manga hero and existing site visual language.
- Link to Bambu Lab Wiki for official follow-up reading; do not copy, download into the repository, or hotlink Wiki images.
- Modify only Course 06 content, the minimum shared rendering support if proven necessary, and focused tests.
- Keep 3DBenchy as an extended diagnostic case, separate from the under-20-minute classroom print.
- Do not add dependencies or refactor the course system.

---

### Task 1: Lock the A1 lesson requirements into the static build

**Files:**
- Modify: `tests/build-site.test.js:27`
- Test: `tests/build-site.test.js`

**Interfaces:**
- Consumes: `buildSite({ rootDir: string, outputDir: string }): { courseCount: number, htmlCount: number }` from `scripts/build-site.js`.
- Produces: A regression test named `publishes the A1 first-print workflow and safety guidance` that validates Course 06's built HTML.

- [ ] **Step 1: Write the failing test**

Insert after the existing static-content test:

```js
test("publishes the A1 first-print workflow and safety guidance", () => {
  const { outputDir } = buildTemporarySite("3d-course-a1-first-print-");
  const lesson = fs.readFileSync(path.join(outputDir, "courses/06-first-print.html"), "utf8");

  for (const expected of [
    "Bambu Lab A1",
    "紋理 PEI 平台",
    "0.4 mm 噴嘴",
    "0.20 mm 標準",
    "自動調平",
    "正常",
    "噴嘴過高",
    "噴嘴過低",
    "未黏住",
    "等待噴頭完全停止",
    "等待平台降溫",
    "20 分鐘內"
  ]) assert.match(lesson, new RegExp(expected));

  assert.match(lesson, /wiki\.bambulab\.com\/en\/p1\/manual\/print-from-bambu-studio/);
  assert.match(lesson, /wiki\.bambulab\.com\/en\/knowledge-sharing\/identify-and-fix-first-layer-issues-with-a-test-print/);
  assert.match(lesson, /wiki\.bambulab\.com\/en\/filament-acc\/acc\/print-finish-adv/);
});
```

- [ ] **Step 2: Run the focused test to verify RED**

Run: `node --test --test-name-pattern="A1 first-print" tests/build-site.test.js`

Expected: FAIL because the current Course 06 static HTML does not contain `Bambu Lab A1` and the required first-layer categories and Wiki links.

- [ ] **Step 3: Commit the failing test**

```bash
git add tests/build-site.test.js
git commit -m "test: define A1 first print lesson"
```

---

### Task 2: Expand Course 06 using existing content components

**Files:**
- Modify: `course-data.js:164-176`
- Test: `tests/build-site.test.js`

**Interfaces:**
- Consumes: Existing Course 06 fields plus section fields `steps: string[]`, `compareHeaders: string[]`, `compare: string[][]`, `callout: string`, and `sources: { label: string, url: string }[]`.
- Produces: A five-section Course 06 rendered identically by browser JavaScript and the static site builder, without a new renderer or asset.

- [ ] **Step 1: Replace only the Course 06 data block**

Use these section responsibilities and exact source URLs:

```js
sections:[
  {
    title:"列印前檢查：先把條件固定",
    body:"第一次列印以教室指定的 Bambu Lab A1 為準。使用 PLA、0.4 mm 噴嘴、紋理 PEI 平台與 0.20 mm 標準預設；送出前確認平台空白、鋼板貼平，切片預覽沒有空中起印或超出平台。",
    points:["印表機：Bambu Lab A1","線材：PLA","噴嘴：0.4 mm","平台：紋理 PEI 平台","列印設定：0.20 mm 標準"]
  },
  {
    title:"從 Bambu Studio 傳送到 A1",
    body:"完成切片與預覽後按下列印，核對目標機器、平台與線材，再保留自動調平並送出。不同版本的按鈕位置可能改變，因此依功能名稱核對，不靠畫面位置死記。",
    steps:["確認目標印表機顯示為教室指定的 A1。","確認平台類型與機器上的紋理 PEI 平台一致。","確認線材為目前裝入的 PLA。","保留自動調平，送出後留在機器旁等待第一層。"],
    sources:[{label:"Bambu Lab Wiki｜從 Bambu Studio 列印",url:"https://wiki.bambulab.com/en/p1/manual/print-from-bambu-studio"}]
  },
  {
    title:"第一層判讀：看見地基再離開",
    body:"第一層必須連續、平整並黏在平台上。不要只看螢幕顯示正在列印，要親眼確認線條狀態。",
    compareHeaders:["狀況","看起來","代表什麼","現在怎麼做"],
    compare:[
      ["正常","線條相接、表面平整、穩定黏住","高度與附著大致正常","繼續觀察到第一層完成"],
      ["噴嘴過高","線條分開、圓角鬆散、容易被拖走","線材沒有被適度壓在平台上","暫停，交由教師檢查"],
      ["噴嘴過低","表面刮痕、波紋或材料向兩側堆起","噴嘴過度擠壓第一層","暫停，交由教師檢查"],
      ["未黏住","線材捲起、跟著噴嘴移動或堆成亂線","平台附著失敗","立即暫停或停止" ]
    ],
    sources:[{label:"Bambu Lab Wiki｜辨認與修正第一層問題",url:"https://wiki.bambulab.com/en/knowledge-sharing/identify-and-fix-first-layer-issues-with-a-test-print"}]
  },
  {
    title:"異常先停，不伸手救",
    body:"發現亂線、拖料或作品鬆脫時，先在 A1 或 Bambu Studio 按暫停；若作品已無法繼續，再停止工作。等待噴頭完全停止，並確認高溫與移動風險後才由教師處理。",
    callout:"暫停不等於噴嘴與平台已冷卻。不要徒手靠近移動中的噴頭，也不要直接在熱床上拉扯纏料；無法判斷時交由教師處理。"
  },
  {
    title:"完成後冷卻取件",
    body:"列印完成後等待平台降溫，再取下彈簧鋼板並輕微彎曲，讓作品自然鬆開。不要過度彎折鋼板；刮刀不是初學者的優先方法，必須使用時刀口不可朝向手掌。",
    steps:["確認畫面顯示工作完成。","等待平台降溫，不急著徒手拉作品。","握住鋼板把手並取下鋼板。","輕微彎曲鋼板，讓作品自然鬆開。","移除裙邊並依教室規則清潔平台。"],
    sources:[{label:"Bambu Lab Wiki｜列印完成後的取件與清潔",url:"https://wiki.bambulab.com/en/filament-acc/acc/print-finish-adv"}]
  }
]
```

Also update the Course 06 lead, goals, manga alt/caption/guides, 3DBenchy `why`, task, and checkpoint so they consistently describe A1, safe stopping, a teacher-selected under-20-minute PLA model, and cooled removal. Do not change Course 05 or Course 07.

- [ ] **Step 2: Run the focused test to verify GREEN**

Run: `node --test --test-name-pattern="A1 first-print" tests/build-site.test.js`

Expected: PASS with one matching test and no failures.

- [ ] **Step 3: Run the full test suite**

Run: `node --test tests/*.test.js`

Expected: All tests pass with zero failures.

- [ ] **Step 4: Commit the lesson implementation**

```bash
git add course-data.js
git commit -m "feat: expand A1 first print lesson"
```

---

### Task 3: Verify the published artifact and prepare review

**Files:**
- Verify: `course-data.js`
- Verify: `courses/06-first-print.html`
- Verify: generated `_site/courses/06-first-print.html`

**Interfaces:**
- Consumes: the Course 06 data and existing static-site builder.
- Produces: a clean branch ready for a draft pull request.

- [ ] **Step 1: Check JavaScript syntax**

Run: `node --check course-data.js && node --check course.js && node --check scripts/build-site.js`

Expected: exit code 0 and no output.

- [ ] **Step 2: Build the complete static site**

Run: `node scripts/build-site.js`

Expected: `Built 12 courses across 13 HTML pages.`

- [ ] **Step 3: Verify the generated Course 06 artifact**

Run:

```bash
rg -n "Bambu Lab A1|噴嘴過高|噴嘴過低|未黏住|等待噴頭完全停止|等待平台降溫|wiki.bambulab.com" _site/courses/06-first-print.html
```

Expected: all seven terms appear in the generated page, including three official Wiki links.

- [ ] **Step 4: Inspect the final scope**

Run: `git status -sb && git diff origin/main...HEAD --check && git diff --stat origin/main...HEAD`

Expected: only the approved specification, plan, focused test, and Course 06 data are changed; no whitespace errors.

- [ ] **Step 5: Push and open a draft pull request**

Push `agent/course-06-a1` to `origin`, then create a draft pull request targeting `main` with a summary of the A1-only lesson, licensing-safe Wiki links, and verification commands.
